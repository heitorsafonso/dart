void main() {
  List<String> list = ['Ricardo', 'Ruan', 'da', 'Silva', 'Lima'];
  //List<String> sublist = list.sublist(2);
  //List<String> sublist = list.sublist(1,3);
  //print(sublist);

  // list.forEach((element) {
  //   print(element);
  // });

  // String procurando = 'Silva';
  // if (list.contains(procurando)) {
  //   print('Achou');
  // } else {
  //   print('Não achou!');
  // }
  //print(list.contains("Silva"));
  //print(!list.contains("Silva"));

  // String myName = list.reduce((value, element) {
  //   return value + " " + element;
  // });
  // print(myName);

  // Iterable maior = list.where((element) => element.length > 4);
  // List<String> maior = list.where((element) => element.length > 4).toList();
  // print(maior);
  // String primeiro = list.firstWhere((element) => element.length > 4);
  // print(primeiro);
  // String ultimo = list.lastWhere((element) => element.length > 4);
  // print(ultimo);
}
